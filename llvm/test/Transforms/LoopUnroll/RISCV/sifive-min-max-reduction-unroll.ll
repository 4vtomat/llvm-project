; NOTE: Assertions have been autogenerated by utils/update_test_checks.py
;RUN: opt -passes=loop-unroll -S -mtriple riscv64-unknown-linux-gnu -mattr="+v" < %s | FileCheck %s

define float @test() {
; CHECK-LABEL: @test(
; CHECK-NEXT:  for.body38:
; CHECK-NEXT:    br label [[VECTOR_BODY:%.*]]
; CHECK:       vector.body:
; CHECK-NEXT:    [[TMP0:%.*]] = call i32 @llvm.vector.reduce.smax.nxv8i32(<vscale x 8 x i32> zeroinitializer)
; CHECK-NEXT:    br label [[VECTOR_BODY]]
;
for.body38:
  br label %vector.body
vector.body:
  %0 = call i32 @llvm.vector.reduce.smax.nxv8i32(<vscale x 8 x i32> zeroinitializer)
  br label %vector.body
}

declare i32 @llvm.vector.reduce.smax.nxv8i32(<vscale x 8 x i32>)
